package step05_Array;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class Quiz06 {
	public static void main(String args[]) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

		/*
		 * int N = Integer.parseInt(br.readLine()); String OX = ""; int[] countArr = new
		 * int[N]; int count = 0; String[] arr = new String[OX.length()]; for (int i =
		 * 0; i < N; i++) { StringTokenizer st = new StringTokenizer(br.readLine()); OX
		 * = br.readLine(); arr = OX.split(""); for (int j = 0; j < OX.length()+1; j++)
		 * { if (arr[j].equals("O")) { count = 1; } if (arr[j].equals(arr[j + 1]) &&
		 * arr[j].equals("O")) { count += 1; countArr[i] = count; } else { count =
		 * countArr[i]; } } }
		 */
	}
}
